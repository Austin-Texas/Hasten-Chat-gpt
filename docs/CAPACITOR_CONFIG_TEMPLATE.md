# Capacitor Config Template

Use this when native packaging begins.

```text
appId: com.hastencargo.driver
appName: HASTEN Cargo
webDir: dist
androidScheme: https
```

## Notes

- Keep the web build output as `dist`.
- Use the final company bundle ID before store submission.
- Confirm app name, icon, splash screen, and privacy policy before release.
